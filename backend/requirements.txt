# Core API (required for demo mode)
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-multipart==0.0.9

# Optional ML stack — the app runs in demo mode without these. Install with
# `pip install -r requirements-ml.txt` once you have real weights / a GPU.
# See requirements-ml.txt for torch, onnxruntime, opencv-python, mediapipe, numpy.
